void main() {
  // = (atribuição)
  // -= (subtração e atribuição)
  // /= (divisão e atribuição)
  // %= (resto da divisão e atribuição)
  // >>=
  // ^=
  // += (soma e atribuição)
  // *= (multiplação e atribuição)
  // ~/=
  // <<=
  // &=
  // |=
  // ??= (caso null faça atribuição)

  var num1 = 2;
  var num2 = 5;

  print('$num1 >>= $num2: ${num1 >> num2}');
}
